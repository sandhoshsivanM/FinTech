<?xml version="1.0" encoding="utf-8"?>
<!-- Pre-API-21 fallback. minSdk is 24, so this is never actually loaded —
     drawable-v21/launch_background.xml is. Kept identical to it so the two
     cannot drift, which is exactly how the stock white background survived
     here long after the app went dark. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher_foreground" />
    </item>
</layer-list>
